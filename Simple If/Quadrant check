# Simple if
# Question:
# Determine the quadrant of a point in a 2D plane based on its coordinates.
# Print "Quadrant I", "Quadrant II", "Quadrant III", or "Quadrant IV" accordingly.

# Answer:
x = float(input("Enter the x-coordinate: "))
y = float(input("Enter the y-coordinate: "))
if x > 0 and y > 0:
    print("Quadrant I")
elif x < 0 and y > 0:
    print("Quadrant II")
elif x < 0 and y < 0:
    print("Quadrant III")
elif x > 0 and y < 0:
    print("Quadrant IV")
else:
    print("On the axes")
